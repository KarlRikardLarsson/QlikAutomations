[
  {
    "qName": "vD_MaxDate",
    "qDefinition": "='$(=RangeMin(Max(Date),Today()))'",
    "description": "The effective current date for the app: the latest date present in the data, capped at the real calendar date so it never shows a future date if data hasn't loaded yet. All other date-driven input variables derive from this.",
    "tags": "time, anchor, core"
  },
  {
    "qName": "vD_R_LY",
    "qDefinition": ">=$(=ADDMONTHS(MAKEDATE($(vD_InputYear)-1,$(vD_InputMonth),1),($1 - 1) * -1))<=$(=MAKEDATE($(vD_InputYear)-1,$(vD_InputMonth),$(vD_InputDay)))",
    "description": "Parameterized rolling N-month window, shifted one year back from the current input month, ending on the same day-of-month as vD_MaxDate rather than the full month end. Call as vD_R_LY(3), vD_R_LY(12), etc. Used as the template behind the *_LY convenience variables. Kept without a leading = or quotes so the $1 parameter substitution still works.",
    "tags": "time, rolling, yoy"
  },
  {
    "qName": "vD_YTD",
    "qDefinition": "='>=$(=MAKEDATE($(vD_InputYear),1,1))<=$(=MakeDate($(vD_InputYear),$(vD_InputMonth),$(vD_InputDay)))'",
    "description": "Year-to-date range for the current input year, from Jan 1 through the exact current day, using day-level precision instead of the end of the current month.",
    "tags": "time, calendar, ytd"
  },
  {
    "qName": "vD_CD",
    "qDefinition": "='$(=DATE($(vD_MaxDate),'DD.MM.YYYY'))'",
    "description": "Current date, formatted as DD.MM.YYYY text, for display in titles and labels. Based on vD_MaxDate rather than the raw system date.",
    "tags": "time, display"
  },
  {
    "qName": "vD_CY",
    "qDefinition": "='>=$(=(MAKEDATE($(vD_InputYear),1,1)))<=$(=(MonthEnd(MakeDate($(vD_InputYear),12,1))))'",
    "description": "Full current calendar year range, Jan 1 to Dec 31, based on the current input year. Always the complete year, so it is not affected by the day-level precision change applied to vD_R and vD_YTD.",
    "tags": "time, calendar"
  },
  {
    "qName": "vD_WeekRollingN",
    "qDefinition": ">=$(=date(weekstart(MAKEDATE($(vD_InputYear),$(vD_InputMonth),$(vD_InputDay)))-(7*$1)))<=$(=date(weekend(MAKEDATE($(vD_InputYear),$(vD_InputMonth),$(vD_InputDay)))))",
    "description": "Parameterized rolling N-week window ending at the end of the current week. Call as vD_WeekRollingN(4) for a trailing 4-week view, etc. Kept without a leading = or quotes so the $1 parameter substitution still works.",
    "tags": "time, rolling"
  },
  {
    "qName": "vD_LM",
    "qDefinition": "='>=$(=ADDMONTHS(MAKEDATE($(vD_InputYear),$(vD_InputMonth),1), -1))<=$(=MonthEnd(ADDMONTHS(MAKEDATE($(vD_InputYear),$(vD_InputMonth),1),-1)))'",
    "description": "Last month: full calendar range for the month immediately preceding the current input month. Kept at full month end intentionally, since last month is already complete and not a to-date period.",
    "tags": "time, calendar"
  },
  {
    "qName": "vD_R",
    "qDefinition": ">=$(=ADDMONTHS(MAKEDATE($(vD_InputYear),$(vD_InputMonth),1),($1 - 1) * -1))<=$(=MAKEDATE($(vD_InputYear),$(vD_InputMonth),$(vD_InputDay)))",
    "description": "Core parameterized rolling N-month window ending on the exact current day (via vD_InputDay) rather than the full month end. Call as vD_R(3), vD_R(6), etc. This is the template underlying vD_R3, vD_R6, vD_R9, vD_R12, and vD_CM. Kept without a leading = or quotes so the $1 parameter substitution still works.",
    "tags": "time, rolling, core"
  },
  {
    "qName": "vD_R12",
    "qDefinition": "='$(vD_R(12))'",
    "description": "Rolling 12-month window ending at the current input day. Convenience wrapper around vD_R(12).",
    "tags": "time, rolling"
  },
  {
    "qName": "vD_R3_LY",
    "qDefinition": "='$(vD_R_LY(3))'",
    "description": "Rolling 3-month window, shifted one year back, ending at the equivalent day last year. Convenience wrapper around vD_R_LY(3).",
    "tags": "time, rolling, yoy"
  },
  {
    "qName": "vD_Range",
    "qDefinition": "=$(=ONLY(Range))",
    "description": "Reads the currently selected value from a Range selector field, e.g. a dropdown of period labels such as R3, YTD, or CM. Intended to drive dynamic period selection in charts, but currently has no dispatcher logic mapping the label to an actual date range variable — flagged for follow-up.",
    "tags": "time, selector"
  },
  {
    "qName": "vD_InputYear",
    "qDefinition": "=Year($(vD_MaxDate))",
    "description": "The active year used throughout all date variables, always derived from vD_MaxDate. No longer tied to a user field selection.",
    "tags": "time, core"
  },
  {
    "qName": "vD_Now",
    "qDefinition": "=Now()",
    "description": "Live system timestamp, date and time, uncapped by data availability. Distinct from vD_MaxDate — use this only where an actual real-time timestamp is needed, e.g. a last-refreshed-at label.",
    "tags": "time, timestamp"
  },
  {
    "qName": "vD_LD",
    "qDefinition": "='$(=DATE($(vD_MaxDate)-1,'$(DateFormat)'))'",
    "description": "Yesterday relative to vD_MaxDate, formatted as DD.MM.YYYY text, for display in titles and labels.",
    "tags": "time, display"
  },
  {
    "qName": "vD_LY",
    "qDefinition": "='>=$(=(MAKEDATE($(vD_InputYear)-1,1,1)))<=$(=(MonthEnd(MakeDate($(vD_InputYear)-1,12,1))))'",
    "description": "Last year: full calendar range, Jan 1 to Dec 31, for the year prior to the current input year. Always the complete year, so it is not affected by the day-level precision change applied to vD_R and vD_YTD.",
    "tags": "time, calendar, yoy"
  },
  {
    "qName": "vD_WeekLastN",
    "qDefinition": ">=$(=date(weekstart(MAKEDATE($(vD_InputYear),$(vD_InputMonth),$(vD_InputDay)))-(7*$1)))<=$(=date(weekend(MAKEDATE($(vD_InputYear),$(vD_InputMonth),$(vD_InputDay)))-(7*$1)))",
    "description": "The single full week that occurred exactly N weeks before the current week, not a rolling range but one specific past week. Call as vD_WeekLastN(1) for last week, vD_WeekLastN(2) for two weeks ago, etc. Kept without a leading = or quotes so the $1 parameter substitution still works.",
    "tags": "time, comparison"
  },
  {
    "qName": "vD_R3",
    "qDefinition": "='$(vD_R(3))'",
    "description": "Rolling 3-month window ending at the current input day. Convenience wrapper around vD_R(3).",
    "tags": "time, rolling"
  },
  {
    "qName": "vD_R6",
    "qDefinition": "='$(vD_R(6))'",
    "description": "Rolling 6-month window ending at the current input day. Convenience wrapper around vD_R(6).",
    "tags": "time, rolling"
  },
  {
    "qName": "vD_R9",
    "qDefinition": "='$(vD_R(9))'",
    "description": "Rolling 9-month window ending at the current input day. Convenience wrapper around vD_R(9).",
    "tags": "time, rolling"
  },
  {
    "qName": "vD_R9_LY",
    "qDefinition": "='$(vD_R_LY(9))'",
    "description": "Rolling 9-month window, shifted one year back, ending at the equivalent day last year. Convenience wrapper around vD_R_LY(9).",
    "tags": "time, rolling, yoy"
  },
  {
    "qName": "vD_InputMonth",
    "qDefinition": "=Num(Month($(vD_MaxDate)))",
    "description": "The active month used throughout all date variables, always derived from vD_MaxDate. Wrapped in Num() because Month() returns a dual value that displays as a month name by default — the numeric form is required for MAKEDATE and ADDMONTHS downstream.",
    "tags": "time, core"
  },
  {
    "qName": "vD_CM_LY",
    "qDefinition": "='$(vD_R_LY(1))'",
    "description": "Current month, shifted one year back, ending at the equivalent day last year, i.e. the same calendar month-to-date last year. Convenience wrapper around vD_R_LY(1).",
    "tags": "time, yoy"
  },
  {
    "qName": "vD_CM",
    "qDefinition": "='$(vD_R(1))'",
    "description": "Current month-to-date range, from the 1st through the current input day. Convenience wrapper around vD_R(1).",
    "tags": "time, calendar"
  },
  {
    "qName": "vD_R6_LY",
    "qDefinition": "='$(vD_R_LY(6))'",
    "description": "Rolling 6-month window, shifted one year back, ending at the equivalent day last year. Convenience wrapper around vD_R_LY(6).",
    "tags": "time, rolling, yoy"
  },
  {
    "qName": "vD_R12_LY",
    "qDefinition": "='$(vD_R_LY(12))'",
    "description": "Rolling 12-month window, shifted one year back, ending at the equivalent day last year. Convenience wrapper around vD_R_LY(12).",
    "tags": "time, rolling, yoy"
  },
  {
    "qName": "vD_RangeCompare",
    "qDefinition": "=$(=ONLY(\"Range Compare\"))",
    "description": "Reads the currently selected value from a Range Compare selector field, intended to pair with vD_Range for period-over-period comparisons, e.g. this R3 vs last year's R3. Same dispatcher gap as vD_Range.",
    "tags": "time, selector, comparison"
  },
  {
    "qName": "vD_DayLastN",
    "qDefinition": ">=$(=date(MAKEDATE($(vD_InputYear),$(vD_InputMonth),$(vD_InputDay))+$1))<=$(=date(MAKEDATE($(vD_InputYear),$(vD_InputMonth),$(vD_InputDay))+$1))",
    "description": "A single day, offset by $1 days from the current input day. Note the definition adds $1 rather than subtracting, so a negative parameter is needed to go backward in time, e.g. vD_DayLastN(-1) for yesterday. Kept without a leading = or quotes so the $1 parameter substitution still works.",
    "tags": "time, day"
  },
  {
    "qName": "vD_DayNofCurrentWeek",
    "qDefinition": ">=$(=date(weekstart(MAKEDATE($(vD_InputYear),$(vD_InputMonth),$(vD_InputDay))) + $1 ))<=$(=date(weekstart(MAKEDATE($(vD_InputYear),$(vD_InputMonth),$(vD_InputDay))) + $1))",
    "description": "A single specific day within the current week, offset $1 days from that week's start, e.g. vD_DayNofCurrentWeek(0) for Monday, vD_DayNofCurrentWeek(2) for Wednesday, depending on week-start convention. Kept without a leading = or quotes so the $1 parameter substitution still works.",
    "tags": "time, day"
  },
  {
    "qName": "vD_WeekLastN_ToDate",
    "qDefinition": ">=$(=date(weekstart(MAKEDATE($(vD_InputYear),$(vD_InputMonth),$(vD_InputDay)))-(7*$1)))<=$(=date(MAKEDATE($(vD_InputYear),$(vD_InputMonth),$(vD_InputDay))-(7*$1)))",
    "description": "The portion of the week N weeks ago, from that week's start through the day-of-week matching today's position: a to-date comparison window rather than the full past week. Kept without a leading = or quotes so the $1 parameter substitution still works.",
    "tags": "time, comparison"
  },
  {
    "qName": "vD_YTD_LY",
    "qDefinition": "='>=$(=MAKEDATE($(vD_InputYear)-1,1,1))<=$(=MakeDate($(vD_InputYear)-1,$(vD_InputMonth),$(vD_InputDay)))'",
    "description": "Year-to-date range for the year prior to the current input year, from Jan 1 through the equivalent day last year, using day-level precision instead of the end of the current month.",
    "tags": "time, ytd, yoy"
  },
  {
    "qName": "vD_InputDay",
    "qDefinition": "=Day($(vD_MaxDate))",
    "description": "The active day used throughout all date variables, always derived from vD_MaxDate. No longer tied to a user field selection.",
    "tags": "time, core"
  },
{
  "qName": "vD_RangeTitle",
  "qDefinition": "Replace(Replace('$1','>=',''),'<=',' - ')",
  "description": "Converts a raw range-string variable such as >=01.06.2026<=31.08.2026 into a readable label like 01.06.2026 - 31.08.2026, by stripping the >= and <= operators and inserting a dash. Call as vD_RangeTitle($(vD_R(3))) or vD_RangeTitle($(vD_YTD)), passing in the expanded range string of whichever period variable you want to label.",
  "tags": "time, display, title"
}
]
